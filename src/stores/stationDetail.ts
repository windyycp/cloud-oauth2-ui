import {defineStore} from 'pinia'
import { ref, computed, watch } from 'vue'

const useChargeDischargeStatusStore = defineStore( "chargeDischargrStatusStore", () => ({
    let chargeDischargrStatusStore = ref([{id:1, title }])

    }),
    actions: {

    }
    
)

export default useChargeDischargeStatusStore;